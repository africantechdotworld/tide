<link
    href="https://fonts.googleapis.com/css2?family=Bricolage+Grotesque:opsz,wght@12..96,300;12..96,400;12..96,500;12..96,600;12..96,700;12..96,800&family=Instrument+Sans:wght@400;500;600&display=swap"
    rel="stylesheet">
<style>
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box
    }

    :root {
        --blue: #1a56db;
        --blue-light: #eff6ff;
        --blue-mid: #dbeafe;
        --blue-dark: #1e3a8a;
        --blue-text: #1d4ed8;
        --gray: #f8fafc;
        --gray2: #f1f5f9;
        --gray3: #e2e8f0;
        --gray4: #94a3b8;
        --gray5: #64748b;
        --gray6: #334155;
        --gray7: #0f172a;
        --white: #ffffff;
        --radius: 14px;
        --radius-sm: 8px;
    }

    body {
        background: var(--white);
        color: var(--gray7);
        font-family: 'Instrument Sans', sans-serif;
        line-height: 1.6;
        overflow-x: hidden
    }

    h1,
    h2,
    h3,
    h4,
    .logo {
        font-family: 'Bricolage Grotesque', sans-serif
    }

    a {
        text-decoration: none;
        color: inherit
    }

    nav {
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
        border-bottom: 1px solid var(--gray3);
        padding: 0 2rem;
        display: flex;
        align-items: center;
        justify-content: space-between;
        height: 68px;
        position: sticky;
        top: 0;
        z-index: 100
    }

    .logo {
        font-size: 1.45rem;
        font-weight: 800;
        color: var(--gray7);
        letter-spacing: -0.5px
    }

    .logo span {
        color: var(--blue)
    }

    .nav-links {
        display: flex;
        gap: 2rem;
        align-items: center
    }

    .nav-links a {
        font-size: 0.875rem;
        color: var(--gray5);
        font-weight: 500;
        transition: color 0.15s
    }

    .nav-links a:hover {
        color: var(--blue)
    }

    .nav-cta {
        background: var(--blue);
        color: var(--white);
        padding: 0.55rem 1.4rem;
        border-radius: var(--radius-sm);
        font-size: 0.875rem;
        font-weight: 600;
        font-family: 'Bricolage Grotesque', sans-serif;
        transition: background 0.15s;
        letter-spacing: 0.01em
    }

    .nav-cta:hover {
        background: var(--blue-dark)
    }

    .hero {
        padding: 6rem 2rem 5rem;
        text-align: center;
        background: var(--white);
        position: relative;
        overflow: hidden
    }

    .hero-bg {
        position: absolute;
        inset: 0;
        background: radial-gradient(ellipse 80% 60% at 50% -10%, #dbeafe 0%, transparent 70%);
        pointer-events: none
    }

    .hero-content {
        position: relative;
        z-index: 1;
        max-width: 820px;
        margin: 0 auto
    }

    .hero-badge {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        background: var(--blue-light);
        border: 1px solid #bfdbfe;
        color: var(--blue-text);
        padding: 0.4rem 1rem;
        border-radius: 100px;
        font-size: 0.78rem;
        font-weight: 600;
        margin-bottom: 1.75rem;
        font-family: 'Bricolage Grotesque', sans-serif;
        letter-spacing: 0.04em;
        text-transform: uppercase
    }

    .hero-badge-dot {
        width: 7px;
        height: 7px;
        background: var(--blue);
        border-radius: 50%;
        animation: blink 2s infinite
    }

    @keyframes blink {

        0%,
        100% {
            opacity: 1
        }

        50% {
            opacity: 0.3
        }
    }

    .hero h1 {
        font-size: clamp(2.6rem, 6vw, 4.2rem);
        font-weight: 800;
        line-height: 1.08;
        letter-spacing: -2px;
        color: var(--gray7);
        margin-bottom: 1.5rem
    }

    .hero h1 em {
        font-style: normal;
        color: var(--blue)
    }

    .hero p {
        font-size: 1.1rem;
        color: var(--gray5);
        max-width: 540px;
        margin: 0 auto 2.5rem;
        font-weight: 400;
        line-height: 1.7
    }

    .hero-btns {
        display: flex;
        gap: 0.85rem;
        justify-content: center;
        flex-wrap: wrap;
        margin-bottom: 3.5rem
    }

    .btn-primary {
        background: var(--blue);
        color: var(--white);
        padding: 0.85rem 2rem;
        border-radius: var(--radius-sm);
        font-weight: 600;
        font-family: 'Bricolage Grotesque', sans-serif;
        font-size: 0.95rem;
        transition: all 0.15s;
        display: inline-block;
        letter-spacing: 0.01em
    }

    .btn-primary:hover {
        background: var(--blue-dark);
        transform: translateY(-1px)
    }

    .btn-ghost {
        border: 1.5px solid var(--gray3);
        color: var(--gray6);
        padding: 0.85rem 2rem;
        border-radius: var(--radius-sm);
        font-weight: 600;
        font-family: 'Bricolage Grotesque', sans-serif;
        font-size: 0.95rem;
        transition: all 0.15s;
        display: inline-block
    }

    .btn-ghost:hover {
        border-color: var(--blue);
        color: var(--blue)
    }

    .hero-stats {
        display: inline-flex;
        gap: 0;
        background: var(--white);
        border: 1px solid var(--gray3);
        border-radius: var(--radius);
        overflow: hidden
    }

    .stat {
        padding: 1rem 2rem;
        border-right: 1px solid var(--gray3);
        text-align: center
    }

    .stat:last-child {
        border-right: none
    }

    .stat-num {
        font-family: 'Bricolage Grotesque', sans-serif;
        font-size: 1.6rem;
        font-weight: 800;
        color: var(--gray7);
        letter-spacing: -1px
    }

    .stat-label {
        font-size: 0.75rem;
        color: var(--gray4);
        margin-top: 2px;
        font-weight: 500
    }

    .hero-visual {
        max-width: 900px;
        margin: 3rem auto 0;
        background: var(--white);
        border: 1px solid var(--gray3);
        border-radius: 16px;
        overflow: hidden
    }

    .browser-bar {
        background: var(--gray2);
        padding: 0.75rem 1rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
        border-bottom: 1px solid var(--gray3)
    }

    .browser-dot {
        width: 10px;
        height: 10px;
        border-radius: 50%
    }

    .browser-url {
        flex: 1;
        background: var(--white);
        border: 1px solid var(--gray3);
        border-radius: 6px;
        padding: 0.25rem 0.75rem;
        font-size: 0.75rem;
        color: var(--gray5);
        margin: 0 0.75rem;
        font-family: monospace
    }

    .terminal {
        background: #0f172a;
        padding: 1.5rem;
        font-family: 'Courier New', monospace;
        font-size: 0.82rem;
        line-height: 1.8;
        min-height: 160px
    }

    .t-dim {
        color: #475569
    }

    .t-green {
        color: #4ade80
    }

    .t-blue {
        color: #60a5fa
    }

    .t-white {
        color: #e2e8f0
    }

    .t-yellow {
        color: #fbbf24
    }

    section {
        padding: 5rem 2rem
    }

    .container {
        max-width: 1100px;
        margin: 0 auto
    }

    .section-label {
        display: inline-block;
        background: var(--blue-light);
        color: var(--blue-text);
        border: 1px solid #bfdbfe;
        padding: 0.3rem 0.85rem;
        border-radius: 100px;
        font-size: 0.72rem;
        font-weight: 700;
        margin-bottom: 1rem;
        font-family: 'Bricolage Grotesque', sans-serif;
        letter-spacing: 0.06em;
        text-transform: uppercase
    }

    .section-title {
        font-size: clamp(1.9rem, 3.5vw, 2.6rem);
        font-weight: 800;
        color: var(--gray7);
        margin-bottom: 1rem;
        letter-spacing: -1px;
        line-height: 1.15
    }

    .section-sub {
        color: var(--gray5);
        font-size: 1rem;
        max-width: 500px;
        line-height: 1.7
    }

    .features-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 1.5rem;
        margin-top: 3rem
    }

    .feature-card {
        background: var(--white);
        border: 1px solid var(--gray3);
        border-radius: var(--radius);
        padding: 1.75rem;
        transition: border-color 0.15s, box-shadow 0.15s
    }

    .feature-card:hover {
        border-color: #bfdbfe;
        box-shadow: 0 4px 20px rgba(26, 86, 219, 0.06)
    }

    .feature-icon {
        width: 46px;
        height: 46px;
        background: var(--blue-light);
        border-radius: 10px;
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 1.1rem
    }

    .feature-icon svg {
        width: 22px;
        height: 22px;
        color: var(--blue)
    }

    .feature-card h3 {
        font-size: 1rem;
        font-weight: 700;
        color: var(--gray7);
        margin-bottom: 0.5rem;
        font-family: 'Bricolage Grotesque', sans-serif
    }

    .feature-card p {
        font-size: 0.875rem;
        color: var(--gray5);
        line-height: 1.65
    }

    .pricing-section {
        background: var(--gray)
    }

    .pricing-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(235px, 1fr));
        gap: 1.25rem;
        margin-top: 3rem
    }

    .plan {
        background: var(--white);
        border: 1.5px solid var(--gray3);
        border-radius: var(--radius);
        padding: 2rem;
        position: relative;
        transition: border-color 0.15s
    }

    .plan.featured {
        border-color: var(--blue);
        box-shadow: 0 0 0 4px rgba(26, 86, 219, 0.07)
    }

    .plan-badge {
        position: absolute;
        top: -13px;
        left: 50%;
        transform: translateX(-50%);
        background: var(--blue);
        color: var(--white);
        padding: 0.25rem 1rem;
        border-radius: 100px;
        font-size: 0.7rem;
        font-weight: 700;
        font-family: 'Bricolage Grotesque', sans-serif;
        white-space: nowrap;
        letter-spacing: 0.04em
    }

    .plan-tier {
        font-size: 0.72rem;
        font-weight: 700;
        color: var(--blue);
        text-transform: uppercase;
        letter-spacing: 0.08em;
        margin-bottom: 0.75rem;
        font-family: 'Bricolage Grotesque', sans-serif
    }

    .plan-price {
        margin-bottom: 0.4rem
    }

    .plan-price .dollar {
        font-size: 1.1rem;
        font-weight: 700;
        color: var(--gray7);
        vertical-align: top;
        line-height: 2.2;
        font-family: 'Bricolage Grotesque', sans-serif
    }

    .plan-price .amount {
        font-size: 3rem;
        font-weight: 800;
        color: var(--gray7);
        letter-spacing: -2px;
        font-family: 'Bricolage Grotesque', sans-serif;
        line-height: 1
    }

    .plan-price .period {
        font-size: 0.85rem;
        color: var(--gray4);
        font-weight: 500
    }

    .plan-desc {
        font-size: 0.82rem;
        color: var(--gray5);
        margin-bottom: 1.5rem;
        padding-bottom: 1.5rem;
        border-bottom: 1px solid var(--gray3);
        line-height: 1.6
    }

    .plan-specs {
        list-style: none;
        margin-bottom: 1.75rem
    }

    .plan-specs li {
        display: flex;
        align-items: center;
        gap: 0.6rem;
        font-size: 0.875rem;
        color: var(--gray6);
        padding: 0.4rem 0
    }

    .plan-specs li svg {
        width: 15px;
        height: 15px;
        color: var(--blue);
        flex-shrink: 0
    }

    .plan-btn {
        display: block;
        text-align: center;
        padding: 0.75rem;
        border-radius: var(--radius-sm);
        font-family: 'Bricolage Grotesque', sans-serif;
        font-size: 0.875rem;
        font-weight: 700;
        transition: all 0.15s;
        letter-spacing: 0.01em
    }

    .plan-btn-outline {
        border: 1.5px solid var(--gray3);
        color: var(--gray6)
    }

    .plan-btn-outline:hover {
        border-color: var(--blue);
        color: var(--blue)
    }

    .plan-btn-filled {
        background: var(--blue);
        color: var(--white)
    }

    .plan-btn-filled:hover {
        background: var(--blue-dark)
    }

    .trust-strip {
        background: var(--white);
        border-top: 1px solid var(--gray3);
        border-bottom: 1px solid var(--gray3);
        padding: 1.5rem 2rem
    }

    .trust-inner {
        max-width: 1100px;
        margin: 0 auto;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 3rem;
        flex-wrap: wrap
    }

    .trust-item {
        display: flex;
        align-items: center;
        gap: 0.6rem;
        font-size: 0.85rem;
        color: var(--gray5);
        font-weight: 500
    }

    .trust-item svg {
        width: 18px;
        height: 18px;
        color: var(--blue)
    }

    .testimonials-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(290px, 1fr));
        gap: 1.25rem;
        margin-top: 3rem
    }

    .tcard {
        background: var(--white);
        border: 1px solid var(--gray3);
        border-radius: var(--radius);
        padding: 1.75rem
    }

    .tcard-stars {
        display: flex;
        gap: 2px;
        margin-bottom: 1rem
    }

    .tcard-stars svg {
        width: 15px;
        height: 15px;
        color: #f59e0b
    }

    .tcard-text {
        font-size: 0.9rem;
        color: var(--gray6);
        line-height: 1.7;
        margin-bottom: 1.25rem;
        font-style: italic
    }

    .tcard-author {
        display: flex;
        align-items: center;
        gap: 0.75rem
    }

    .tcard-avatar {
        width: 38px;
        height: 38px;
        border-radius: 50%;
        background: var(--blue);
        display: flex;
        align-items: center;
        justify-content: center;
        font-family: 'Bricolage Grotesque', sans-serif;
        font-size: 0.8rem;
        font-weight: 700;
        color: var(--white)
    }

    .tcard-name {
        font-size: 0.875rem;
        font-weight: 600;
        color: var(--gray7)
    }

    .tcard-role {
        font-size: 0.775rem;
        color: var(--gray4)
    }

    .faq-section {
        background: var(--gray)
    }

    .faq-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 1rem;
        margin-top: 3rem
    }

    .faq-item {
        background: var(--white);
        border: 1px solid var(--gray3);
        border-radius: var(--radius-sm);
        padding: 1.5rem
    }

    .faq-q {
        font-size: 0.9rem;
        font-weight: 700;
        color: var(--gray7);
        margin-bottom: 0.6rem;
        font-family: 'Bricolage Grotesque', sans-serif
    }

    .faq-a {
        font-size: 0.85rem;
        color: var(--gray5);
        line-height: 1.65
    }

    .cta-section {
        background: var(--blue-dark);
        padding: 5rem 2rem;
        text-align: center
    }

    .cta-section h2 {
        font-size: clamp(2rem, 4vw, 2.8rem);
        font-weight: 800;
        color: var(--white);
        letter-spacing: -1px;
        margin-bottom: 1rem
    }

    .cta-section p {
        color: #bfdbfe;
        font-size: 1rem;
        margin-bottom: 2rem;
        max-width: 480px;
        margin-left: auto;
        margin-right: auto
    }

    .btn-white {
        background: var(--white);
        color: var(--blue-dark);
        padding: 0.9rem 2.25rem;
        border-radius: var(--radius-sm);
        font-weight: 700;
        font-family: 'Bricolage Grotesque', sans-serif;
        font-size: 0.95rem;
        display: inline-block;
        transition: all 0.15s
    }

    .btn-white:hover {
        background: #eff6ff
    }

    footer {
        background: var(--gray7);
        padding: 4rem 2rem 2rem
    }

    .footer-grid {
        display: grid;
        grid-template-columns: 2fr 1fr 1fr 1fr;
        gap: 3rem;
        max-width: 1100px;
        margin: 0 auto;
        padding-bottom: 3rem;
        border-bottom: 1px solid #1e293b
    }

    .footer-logo {
        font-family: 'Bricolage Grotesque', sans-serif;
        font-size: 1.35rem;
        font-weight: 800;
        color: var(--white);
        margin-bottom: 1rem;
        letter-spacing: -0.5px
    }

    .footer-logo span {
        color: #60a5fa
    }

    .footer-brand p {
        font-size: 0.85rem;
        color: #64748b;
        line-height: 1.7;
        max-width: 240px
    }

    .footer-col h4 {
        font-family: 'Bricolage Grotesque', sans-serif;
        font-size: 0.7rem;
        font-weight: 700;
        color: #475569;
        text-transform: uppercase;
        letter-spacing: 0.1em;
        margin-bottom: 1rem
    }

    .footer-col a {
        display: block;
        font-size: 0.85rem;
        color: #64748b;
        margin-bottom: 0.5rem;
        transition: color 0.15s
    }

    .footer-col a:hover {
        color: #93c5fd
    }

    .footer-bottom {
        max-width: 1100px;
        margin: 2rem auto 0;
        display: flex;
        justify-content: space-between;
        align-items: center;
        flex-wrap: wrap;
        gap: 1rem
    }

    .footer-bottom p {
        font-size: 0.8rem;
        color: #475569
    }

    .uptime-bar {
        display: flex;
        gap: 3px
    }

    .uptime-seg {
        height: 4px;
        width: 8px;
        border-radius: 2px;
        background: #4ade80
    }

    .uptime-seg.x {
        background: #374151
    }

    @media(max-width:768px) {
        .nav-links {
            display: none
        }

        .hero-stats {
            flex-direction: column
        }

        .stat {
            border-right: none;
            border-bottom: 1px solid var(--gray3)
        }

        .stat:last-child {
            border-bottom: none
        }

        .faq-grid {
            grid-template-columns: 1fr
        }

        .footer-grid {
            grid-template-columns: 1fr 1fr
        }
    }

    svg.icon {
        display: inline-block;
        vertical-align: middle
    }
</style>

<nav>
    <div class="logo">Veltra<span>Host</span></div>
    <div class="nav-links">
        <a href="#features">Features</a>
        <a href="#pricing">Pricing</a>
        <a href="#reviews">Reviews</a>
        <a href="#faq">FAQ</a>
        <a href="{{ 'login'|link }}">Login</a>
    </div>
    <a href="{{ 'order'|link }}" class="nav-cta">Deploy Now</a>
</nav>

<section class="hero">
    <div class="hero-bg"></div>
    <div class="hero-content">
        <div class="hero-badge">
            <span class="hero-badge-dot"></span>
            Enterprise Cloud Infrastructure
        </div>
        <h1>The fastest VPS hosting<br>for <em>serious builders</em></h1>
        <p>Deploy your apps on NVMe SSD servers across Europe and the US. Full root access, any OS, instant
            provisioning.</p>
        <div class="hero-btns">
            <a href="{{ 'login'|link }}" class="btn-primary">Start for $10 / month</a>
            <a href="#pricing" class="btn-ghost">See all plans</a>
        </div>
        <div class="hero-stats">
            <div class="stat">
                <div class="stat-num">99.9%</div>
                <div class="stat-label">Uptime SLA</div>
            </div>
            <div class="stat">
                <div class="stat-num">&lt;20ms</div>
                <div class="stat-label">EU Latency</div>
            </div>
            <div class="stat">
                <div class="stat-num">NVMe</div>
                <div class="stat-label">SSD Storage</div>
            </div>
            <div class="stat">
                <div class="stat-num">24/7</div>
                <div class="stat-label">Support</div>
            </div>
        </div>
    </div>
</section>

<div class="trust-strip">
    <div class="trust-inner">
        <div class="trust-item">
            DDoS Protection Included
        </div>
        <div class="trust-item">
            NVMe SSD Storage
        </div>
        <div class="trust-item">
            Instant Provisioning
        </div>
        <div class="trust-item">
            EU & US Data Centers
        </div>
        <div class="trust-item">
            Full Root Access
        </div>
    </div>
</div>

<section id="features">
    <div class="container">
        <span class="section-label">Why VeltraHost</span>
        <h2 class="section-title">Built for performance.<br>Priced for everyone.</h2>
        <div class="features-grid">
            <div class="feature-card">
                <h3>NVMe SSD Storage</h3>
                <p>Ultra-fast NVMe drives deliver 3x faster read/write speeds than standard SSDs.</p>
            </div>
            <div class="feature-card">
                <h3>EU & US Data Centers</h3>
                <p>Servers in Nuremberg and Ashburn. Sub-20ms latency for EU clients.</p>
            </div>
            <div class="feature-card">
                <h3>DDoS Protection</h3>
                <p>Enterprise-grade DDoS mitigation on every plan.</p>
            </div>
        </div>
    </div>
</section>

<section id="pricing" class="pricing-section">
    <div class="container">
        <h2 class="section-title">Simple, transparent pricing</h2>
        <div class="pricing-grid">
            <div class="plan">
                <div class="plan-tier">Starter</div>
                <div class="plan-price">$10<span>/mo</span></div>
                <a href="{{ 'order'|link }}" class="plan-btn plan-btn-outline">Get started</a>
            </div>
            <div class="plan featured">
                <div class="plan-tier">Business</div>
                <div class="plan-price">$20<span>/mo</span></div>
                <a href="{{ 'order'|link }}" class="plan-btn plan-btn-filled">Get started</a>
            </div>
        </div>
    </div>
</section>

<footer>
    <div class="footer-grid">
        <div class="footer-brand">
            <div class="footer-logo">Veltra<span>Host</span></div>
            <p>© 2026 VeltraHost. All rights reserved.</p>
        </div>
    </div>
</footer>
